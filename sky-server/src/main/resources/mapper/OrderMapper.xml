<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderMapper">

    <!--插入订单-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into orders (number, order_time, pay_status, status, phone, consignee,amount, address, user_id, address_book_id)
        values (#{number}, #{orderTime}, #{payStatus}, #{status}, #{phone}, #{consignee},#{amount}, #{address}, #{userId}, #{addressBookId})
    </insert>
</mapper>